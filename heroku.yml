build:
  web:
    dockerfile: Deploy/Heroku/web.Dockerfile
  worker:
    dockerfile: Deploy/Heroku/worker.Dockerfile
  release:
    dockerfile: Deploy/Heroku/release.Dockerfile
run:
  web:
    image: web
  worker:
    image: worker
release:
  image: release
